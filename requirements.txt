# Random Forest + XGBoost Forex Prediction Requirements
# Supports: Random Forest (CPU) direction prediction + XGBoost (GPU) price prediction

# ============================================================================
# CORE DATA PROCESSING & ANALYSIS
# ============================================================================
pandas>=1.5.0
numpy>=1.24.0
tqdm>=4.65.0

# ============================================================================
# MACHINE LEARNING - CORE MODELS
# ============================================================================
# Traditional ML (Random Forest - CPU)
scikit-learn>=1.3.0
joblib>=1.3.0

# Gradient Boosting with GPU support (XGBoost - GPU)
xgboost>=1.7.0

# ============================================================================
# VISUALIZATION & REPORTING
# ============================================================================
matplotlib>=3.6.0
seaborn>=0.12.0

# ============================================================================
# MODEL EXPORT & DEPLOYMENT (MetaTrader 5 Integration)
# ============================================================================
onnx>=1.14.0
onnxruntime>=1.15.0
skl2onnx>=1.11.0

# ============================================================================
# TRADING PLATFORM (Optional - for live data)
# ============================================================================
MetaTrader5>=5.0.37

# ============================================================================
# GPU TESTING & VALIDATION (Optional)
# ============================================================================
# PyTorch for GPU testing only (used in test_gpu.py)
torch>=2.0.0

# ============================================================================
# INSTALLATION INSTRUCTIONS
# ============================================================================

# Basic Installation (CPU only):
# pip install pandas numpy scikit-learn matplotlib joblib onnx onnxruntime skl2onnx

# Recommended Installation (with GPU support):
# pip install -r requirements.txt

# XGBoost GPU Notes:
# - Requires CUDA toolkit (11.8+ recommended)
# - Install XGBoost with: pip install xgboost
# - GPU support is automatically detected
# - Falls back to CPU if GPU unavailable

# PyTorch GPU (for testing only):
# pip install torch --index-url https://download.pytorch.org/whl/cu118

# Minimal Installation (basic functionality):
# pip install -r requirements_basic.txt